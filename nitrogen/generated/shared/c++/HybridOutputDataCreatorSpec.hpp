///
/// HybridOutputDataCreatorSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `OutputData` to properly resolve imports.
namespace margelo::nitro::cashudevkit { struct OutputData; }
// Forward declaration of `KeyEntry` to properly resolve imports.
namespace margelo::nitro::cashudevkit { struct KeyEntry; }
// Forward declaration of `P2PKOptions` to properly resolve imports.
namespace margelo::nitro::cashudevkit { struct P2PKOptions; }

#include "OutputData.hpp"
#include <string>
#include <vector>
#include "KeyEntry.hpp"
#include <optional>
#include "P2PKOptions.hpp"
#include <NitroModules/ArrayBuffer.hpp>

namespace margelo::nitro::cashudevkit {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `OutputDataCreator`
   * Inherit this class to create instances of `HybridOutputDataCreatorSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridOutputDataCreator: public HybridOutputDataCreatorSpec {
   * public:
   *   HybridOutputDataCreator(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridOutputDataCreatorSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridOutputDataCreatorSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridOutputDataCreatorSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual OutputData createSingleRandomData(double amount, const std::string& keysetId) = 0;
      virtual std::vector<OutputData> createRandomData(double amount, const std::string& keysetId, const std::vector<KeyEntry>& keys, const std::optional<std::vector<double>>& customSplit) = 0;
      virtual OutputData createSingleP2PKData(const P2PKOptions& p2pk, double amount, const std::string& keysetId) = 0;
      virtual std::vector<OutputData> createP2PKData(const P2PKOptions& p2pk, double amount, const std::string& keysetId, const std::vector<KeyEntry>& keys, const std::optional<std::vector<double>>& customSplit) = 0;
      virtual OutputData createSingleDeterministicData(double amount, const std::shared_ptr<ArrayBuffer>& seed, double counter, const std::string& keysetId) = 0;
      virtual std::vector<OutputData> createDeterministicData(double amount, const std::shared_ptr<ArrayBuffer>& seed, double counter, const std::string& keysetId, const std::vector<KeyEntry>& keys, const std::optional<std::vector<double>>& customSplit) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "OutputDataCreator";
  };

} // namespace margelo::nitro::cashudevkit
